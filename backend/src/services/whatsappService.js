const axios = require('axios');
const env = require('../config/env');

exports.sendTemplateMessage = async (toPhone, message) => {
  // Placeholder: implement WhatsApp Cloud API call
  console.log(`(whatsapp) would send to ${toPhone}: ${message}`);
  // Example POST request using axios to WhatsApp Cloud API can be added here.
};
