const env = require('../config/env');

exports.callWithVoice = async (toPhone, message) => {
  // Placeholder: integrate Twilio to call and play TTS audio
  console.log(`(call) would call ${toPhone} and play: ${message}`);
};
