const env = require('../config/env');

exports.generateTTS = async (text, filename) => {
  // Placeholder: call ElevenLabs / Google TTS to generate audio and save to disk
  console.log(`(voice) would generate TTS for: "${text}" -> ${filename}`);
};
